# Provider: anthropic (default) | openai
AITEAM_PROVIDER=anthropic

# Optional model override (defaults: anthropic → claude-opus-4-8, openai → gpt-4o)
# AITEAM_MODEL=claude-opus-4-8

# Credentials — set the one matching AITEAM_PROVIDER
ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
